import { describe, expect, it } from 'vitest';
import { buildWorkoutFromRoutine } from './workout-builder.js';
import { logSet, progress, toHistoryEntry } from './workout-logging.js';
describe('Workout logging', () => { it('validates and logs reps sets', () => { const w = buildWorkoutFromRoutine({ id: 'r', name: 'Сила', ex: [{ id: 'e', sets: 1, reps: 8, weight: 20 }] }, 1); const next = logSet(w, 0, 0, { w: 22.5, r: 8, done: true }); expect(progress(next)).toEqual({ total: 1, done: 1, percent: 100 }); expect(toHistoryEntry({ ...next, end: 2 }, '2026-08-27').entries[0].sets[0]).toEqual({ w: 22.5, r: 8 }); }); it('supports timed and cardio validation', () => { const timed = buildWorkoutFromRoutine({ id: 't', ex: [{ id: 't1', mode: 'time', sets: 1, sec: 30 }] }); const cardio = buildWorkoutFromRoutine({ id: 'c', ex: [{ id: 'c1', mode: 'cardio', sets: 1, min: 10, speed: 8 }] }); expect(progress(logSet(timed, 0, 0, { sec: 35, done: true })).done).toBe(1); expect(progress(logSet(cardio, 0, 0, { min: 12, speed: 9, done: true })).percent).toBe(100); expect(() => logSet(timed, 0, 0, { sec: 0 })).toThrow(/invalid/); }); });
