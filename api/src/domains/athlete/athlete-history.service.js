export class AthleteHistoryService { constructor({ pgRepository, stateRepository }) { this.pgRepository = pgRepository; this.stateRepository = stateRepository; } async list(actor, options) { if (this.pgRepository) return this.pgRepository.list(actor.id, options); return this.stateRepository.get(actor.id).then(state => state?.workouts || []); } async save(actor, workout) { if (this.pgRepository) return this.pgRepository.save(actor.id, workout); return null; } }
