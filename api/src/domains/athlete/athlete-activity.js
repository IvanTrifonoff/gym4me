const MAX_NAME = 60;
export function activityDto(body = {}) { return { active: Boolean(body.active), name: String(body.name || '').slice(0, MAX_NAME), exerciseIndex: Math.max(0, Number(body.exIdx) || 0), exerciseTotal: Math.max(0, Number(body.exTotal) || 0), setsDone: Math.max(0, Number(body.setsDone) || 0), setsTotal: Math.max(0, Number(body.setsTotal) || 0), startedAt: Math.max(0, Number(body.startedAt) || Date.now()) }; }
export class AthleteActivityRepository { constructor() { this.items = new Map(); } set(athleteId, body) { const item = activityDto(body); if (item.active) this.items.set(String(athleteId), { ...item, updatedAt: Date.now() }); else this.items.delete(String(athleteId)); return item; } get(athleteId) { return this.items.get(String(athleteId)) || null; } }
